global:
  scrape_interval: 15s

rule_files:
  - "alerts.yml"

scrape_configs:
  - job_name: 'tagt-backend'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['host.docker.internal:5000']
    # If using ADMIN_KEY authorization on /metrics
    # bearer_token: "YOUR_ADMIN_KEY"
