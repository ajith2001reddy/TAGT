import BlogClient from "./BlogClient";

export const metadata = {
    title: "Blog - TAGT",
    description: "Read the latest news and updates from TAGT.",
};

export default function BlogPage() {
    return <BlogClient />;
}
