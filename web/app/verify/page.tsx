import VerifyClient from "./VerifyClient";

export const metadata = {
    title: "Verify Identity - TAGT Framework",
    description: "Verify your identity on the TAGT Platform.",
};

export default function VerificationPage() {
    return <VerifyClient />;
}
