import LoginClient from "./LoginClient";

export const metadata = {
    title: "Login - TAGT Framework",
    description: "Sign in to your TAGT account.",
};

export default function LoginPage() {
    return <LoginClient />;
}