import SignupClient from "./SignupClient";

export const metadata = {
    title: "Sign Up - TAGT Framework",
    description: "Create your TAGT account.",
};

export default function SignupPage() {
    return <SignupClient />;
}